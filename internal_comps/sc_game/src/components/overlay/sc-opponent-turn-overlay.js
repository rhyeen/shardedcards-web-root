import { LitElement, html } from '@polymer/lit-element';

export class ScOpponentTurnOverlay extends LitElement {
  render() {
    return html``;
  }
}

window.customElements.define('sc-opponent-turn-overlay', ScOpponentTurnOverlay);
